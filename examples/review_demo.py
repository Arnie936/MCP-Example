"""Small example used to demonstrate Codex pull request review."""


def average(values: list[float]) -> float:
    """Return the arithmetic mean, or 0.0 when values is empty."""
    return sum(values) / len(values)
